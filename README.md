# Graph Lint

Graph Lint — плагин для Obsidian, который автоматически проставляет связи между заметками.

## Возможности

- Автоматическое обновление **ссылок** между заметками в двух графах: Void и Celestia
- Поддержка периодических заметок: **daily, weekly, monthly, quarterly, yearly**
- Создание **событий** (Сон, Мысль, Анализ, Самоанализ) на основе активной дневной заметки
- Построение иерархии связей: **founder → father → ancestor** с настраиваемыми алиасами (herald / bubble / arm и т.д.)
- Автоматическое добавление **тегов**

## Требования

- Obsidian 1.0+
- Node.js и npm для сборки
- Dev-зависимости: TypeScript, esbuild, ESLint, Prettier (см. `package.json`)

## Особенности

### Заметки

Имя заметки выглядит как набора фраз, разделённых точками. Фраза может состоять как из одного слова, так и из нескольких. Например: `go.типы_данных.int`. Не обязательно использовать маленькие буквы и разделять слова нижними подчёркиваниями, но я пришёл к такому для единообразия вида.

Для заметки автоматически на основе её имени генерируется "шапка", состоящая из набора ссылок. Весь основной текст заметки должен идти после заголовка первого уровня (он, соответственно, должен быть единственным). Этот заголовок автоматически генерируется по последней фразы имени заметки.

Все заметки хранилища разделяются на две папки:

### Папка Celestia

Папка для доменных заметок, которые разделяют хранилище на основные категории. Все категории растут из четырёх основных:

- ❤️‍🔥 (fire): реализация - сюда я отрошу любое планирование, в том числе периодические заметки, учёба, работа
- 🌬️ (air): саморазвитие - сюда я отношу вещи, которые мне интересны как источник знаний: книги, фильмы, конспекты, саморазвитие
- 🪨 (earth): личное - сюда я отношу вещи, касающиеся быта или близких людей
- 🌊 (water): духовное - сюда я отношу вещи, касающиеся только меня - заметки о мыслях, снах

От них исходят заметки-категории. Например: `❤️‍🔥.календарь.периодические.периодическая.daily`

### Папка Void

Папка для основного пласта заметок. Требования к заметкам - первая фраза любой заметки должна совпадать с последней фразой какой-нибудь заметки из `Celestia`. То есть они как бы "склеиваются". Например, в `Celestia` есть категория `🌊.сны.сон`, а в `Void` к ней "приклеивается" заметка `сон.2024-09-14.1`

### Связи

Существует три вида связей между заметками:

- `founder` - основатель заметки; для заметок из нескольких фраз это заметка, чьё имя совпадает с первой фразой этой заметки; для заметки из одной фразы это заметка, соответствующая ей в `Celestia`
- `father` - отец заметки; для заметок из нескольких фраз это заметка, чьё имя совпадает с именем текущей, но без последней фразы; для заметки из одной фразы совпадает с `founder`
- `ancestor` - любые специальные заметки, например, для заметок-снов это может быть дата из имени заметки

### Теги

Тег генерируется на основе основной категории. Например, для void-заметки `астрология.дома.дом.1` с соответствующей ей celestia-заметки `🌬️.эзотерика.астрология` тег - саморазвитие(🌬️).

## Установка из исходников

1. Переместить `main.js` и `manifest.json` в `{path-to-your-vault}/.obsidian/plugins/obsidian-graphlint`
2. В Obsidian: Settings → Community Plugins → включить плагин **Graph Lint**

## Настройки

Открываются в Obsidian: Settings → Graph Lint

### Пути

- **Celestia path** — путь к `Celestia` (по умолчанию `master/<-9> celestia/`)
- **Void path** — путь к папке с `Void` (по умолчанию `master/<9> void/`)

### Алиасы

Для каждого графа задаются алиасы связей.

Значения по умолчанию:

- Void: `herald` / `bubble` / `arm`
- Celestia: `archont` / `band` / `mate`

### Маппинг тегов

Значения по умолчанию:

- `❤️‍🔥` → реализация
- `🪨` → личное
- `🌊` → духовность
- `🌬️` → саморазвитие

### Периодические шаблоны и Celestia-пути

Шаблоны имён периодических заметок:

- `daily`: `0000-00-00`
- `weekly`: `0000-W00`
- `monthly`: `0000-00`
- `quarterly`: `0000-Q0`
- `yearly`: `0000`

Celestia-пути для соответствующих календарных заметок (например `❤️‍🔥.календарь.периодические.периодическая.daily`).

### Типы событий

Список дополнений к ежедневным заметкам

- По умолчанию: `Сон`, `Мысль`, `Анализ`, `Самоанализ`

## Команды

Доступны через Command Palette Obsidian:

- **Update Links of Note** — обновить ссылки только для активной заметки
- **Update Links of Vault** — обновить ссылки для всех заметок
- **Create New Periodic Event** — создать новое событие для текущей дневной заметки
- **Add Relative Note** — добавить родственника (ребёнка, отца или брата) к активной заметке; для брата копирует структуру (заголовки)

## Логи и отладка

Плагин пишет в консоль. Так что вы всегда сможете проследить за тем, что было выполнено, а что пошло не так.

Для отладки:

1. Открыть Developer Tools в Obsidian
2. Перейти на вкладку Console
