# Graph Lint

Graph Lint — плагин для Obsidian, который автоматически проставляет связи между ежедневными/периодическими заметками и событиями (сны, мысли и т.п.) в вашей системе Void/Celestia.

## Возможности

- Автоматическое обновление **ссылок** между заметками в двух графах: Void и Celestia
- Поддержка периодических заметок: **daily, weekly, monthly, quarterly, yearly**
- Создание **событий** (Сон, Мысль, Анализ, Самоанализ) на основе активной дневной заметки
- Построение иерархии связей: **founder → father → ancestor** с настраиваемыми алиасами (herald / bubble / arm и т.д.)
- Автоматическое добавление **тегов** на основе корневой ноты и маппинга эмодзи → тег

## Требования

- Obsidian 1.0+
- Node.js и npm для сборки
- Dev-зависимости: TypeScript, esbuild, ESLint, Prettier (см. `package.json`)

## Установка из исходников

1. Установить зависимости:

   ```bash
   npm install
   ```

2. Собрать плагин:

   ```bash
   npm run build
   ```

   Это создаст `main.js` и `manifest.json` в корне плагина.

3. Скопировать папку плагина в:

   ```text
   <your-vault>/.obsidian/plugins/periodic-notes-linker/
   ```

4. В Obsidian: Settings → Community Plugins → включить **Graph Lint**

## Настройки

Открываются в Obsidian: Settings → Graph Lint

### Пути

- **Void path** — путь к папке с Void-заметками (по умолчанию `master/<9> void/`)
- **Celestia path** — путь к Celestia (по умолчанию `master/<-9> celestia/`)

### Алиасы

Для каждого графа задаются алиасы ролей:

- `founder` — основатель
- `ancestor` — предок
- `father` — родитель

Значения по умолчанию:

- Void: `herald` / `bubble` / `arm`
- Celestia: `archont` / `band` / `mate`

### Маппинг тегов

Маппинг эмодзи → текстового тега:

- `❤️‍🔥` → реализация
- `🪨` → личное
- `🌊` → духовность
- `🌬️` → саморазвитие

Редактируется в настройках.

### Периодические шаблоны и Celestia-пути

Шаблоны имён периодических заметок:

- `daily`: `0000-00-00`
- `weekly`: `0000-W00`
- `monthly`: `0000-00`
- `quarterly`: `0000-Q0`
- `yearly`: `0000`

Celestia-пути для соответствующих календарных заметок (например `❤️‍🔥.календарь.периодические.периодическая.daily`).

### Типы событий

Список типов событий, используемый командой создания нового события:

- По умолчанию: `Сон`, `Мысль`, `Анализ`, `Самоанализ`

## Как это работает

### Структура ноты

Класс `Note`:

- `name` — полное имя файла без расширения (например `go.2024-09-31.mama`)
- `words` — массив слов из `name`, разделённых точкой (в примере `["go", "2024-09-31", "mama"]`)
- `title` — последнее слово (`mama` или `2024-09-31` для датовых файлов)
- `body` — содержимое файла после заголовка

### Определение типов

Класс `Validator`:

- `isDaily` — `YYYY-MM-DD`
- `isWeekly` — `YYYY-Www`
- `isMonthly` — `YYYY-MM`
- `isQuarterly` — `YYYY-Qx`
- `isYearly` — `YYYY`
- `isDream` — `title` начинается с `сон.`
- `isThought` — `title` начинается с `мысль.`

`createNote(title, text, plugin)` выбирает соответствующий класс (`Daily`, `Weekly`, `Monthly`, `Quarterly`, `Yearly`, `Dream`, `Thought`) или возвращает базовый `Note`.

### Founder / Father / Ancestor

В `Note`:

- **founder** — первое слово имени: `this.words[0]`
- **father**:
  - если `words.length > 1` → все слова кроме последнего, соединённые точкой (`go.2024-09-31` для `go.2024-09-31.mama`)
  - иначе → совпадает с founder

В `Linker`:

- Поиск founder/father через `Repo.find` в нужном графе (Void / Celestia, с учётом шаблонов периодических заметок)
- Формирование ссылок:

  ```md
  [herald:: [[go#go|go]]]
  [arm:: [[go.2024-09-31#2024-09-31|2024-09-31]]]
  ```

- Генерация ancestor-ссылок по дате:
  - Для периодических заметок — цепочка вверх (неделя / месяц / квартал / год), **без ссылки на саму себя**
  - Для обычных заметок с датой в имени — берётся один уровень (день → неделя → месяц → квартал) по приоритету

### Теги

Класс `Tager`:

1. Находит founder-заметку
2. Для неё находит «корневую» заметку в Celestia
3. Проверяет её содержимое на эмодзи-ключи из `settings.tags.mapping`
4. Возвращает тег (строку), который добавляется к заметке

### Обновление всех ссылок

Функция `updateAllLinks(plugin)`:

1. Собирает все Markdown-файлы хранилища
2. Делит их на:
   - Void (путь начинается с `paths.void`)
   - Celestia (путь начинается с `paths.celestia`)
3. Строит графы `VOID` и `CELESTIA` через `Repo.createGraph`
4. Сортирует `CELESTIA.files` по имени
5. Обновляет **все Celestia-файлы последовательно**:

   ```ts
   for (const file of CELESTIA.files) {
     await updater.update(file, CELESTIA, CELESTIA);
   }
   ```

6. Делит Void на две группы:
   - **корневые** (имя без точек)
   - **вложенные** (имя содержит точки)
7. Обновляет:
   - сначала все корневые Void-файлы параллельно
   - затем все вложенные Void-файлы параллельно (часто батчами для стабильности)

## Команды

Доступны через Command Palette Obsidian:

- **Update Links of Note** — обновить ссылки только для активной заметки
- **Update Links of Vault** — обновить ссылки для всех заметок (Celestia → Void)
- **Create New Periodic Event** — создать новое событие (сон, мысль, и т.п.) для текущей дневной заметки

## Примеры

### Периодическая дневная заметка

Имя: `2024-09-31`

- Тип: `Daily`
- Связи:
  - founder/father в Celestia по шаблонам
  - ancestor: неделя, месяц, квартал, год (без ссылки на сам день)

### Событие-сон

Имя: `сон.2022-07-18.<1>`

- Тип: `Dream`
- Founder: `сон`
- Father: `сон.2022-07-18`
- Ancestor: дата `2022-07-18` (и выше по датовой цепочке, если существуют соответствующие периодические заметки)

## Сборка и разработка

Скрипты в `package.json`:

```bash
npm run dev        # dev-сборка (watch)
npm run build      # production-сборка
npm run lint       # проверка ESLint
npm run lintfix    # ESLint с авто-фиксом
npm run format     # форматирование Prettier
npm run formatcheck # проверка форматирования
```

Сборка выполняется через `esbuild.config.mjs` (вход `src/main.ts`, выход `main.js`).

Конфиги:

- `.eslintrc.json`, `.eslintignore`
- `.prettierrc.json`, `.prettierignore`
- `tsconfig.json`

## Логи и отладка

Плагин пишет в консоль:

- инфо-логи о запуске команд и стадиях обновления (Celestia, корневые Void, вложенные Void)
- при включённом debug — подробные `console.debug` по батчам или отдельным файлам

Для отладки:

1. Открыть Developer Tools в Obsidian
2. Перейти на вкладку Console
3. Отфильтровать сообщения по уровню (Errors / Warnings / Info / Verbose)

## Лицензия

MIT
